version: '3.8'

services:
  web:
    build: .
    container_name: zemer-web
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - zemer-network

networks:
  zemer-network:
    driver: bridge
